version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: skillsfinder-backend
    environment:
      - DB_HOST=106.14.112.139
      - DB_USER=agent1223
      - DB_PWD=agent1223
      - REDIS_HOST=106.14.112.139
      - REDIS_PWD=
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: skillsfinder-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
